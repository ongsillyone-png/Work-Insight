<nav class="app-header navbar navbar-expand navbar-light px-3">
  <div class="container-fluid d-flex align-items-center justify-content-between">
    <!-- Left navbar links -->
    <div class="d-flex align-items-center gap-2">
      <button class="btn d-lg-none d-flex align-items-center justify-content-center rounded-circle" onclick="document.body.classList.toggle('sidebar-open')" type="button" style="width: 38px; height: 38px; border: none; background-color: var(--hospital-teal-light); color: var(--hospital-teal-dark); transition: all 0.2s;">
        <i class="bi bi-list fs-4"></i>
      </button>
      <span class="fw-semibold text-secondary d-none d-md-inline" style="font-size: 0.9rem;">
        ระบบบันทึกกิจกรรมดิจิทัลประจำวัน • <span class="text-teal">Work Insight</span>
      </span>
    </div>

    <!-- Right navbar links -->
    <div class="d-flex align-items-center gap-3">
      <!-- Notifications -->
      <div class="dropdown">
        <a class="btn btn-light btn-sm rounded-circle p-2 position-relative" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="bi bi-bell fs-5 text-secondary"></i>
          <span class="position-absolute top-0 start-100 translate-middle p-1.5 bg-danger border border-light rounded-circle">
            <span class="visually-hidden">แจ้งเตือนใหม่</span>
          </span>
        </a>
        <div class="dropdown-menu dropdown-menu-end shadow border-0 rounded-card p-3" style="width: 280px;">
          <h6 class="fw-bold text-dark mb-3">การแจ้งเตือน</h6>
          <div class="d-flex flex-column gap-2">
            <div class="d-flex gap-2 p-2 rounded hover-bg-light small">
              <i class="bi bi-info-circle text-teal mt-0.5"></i>
              <div>
                <p class="mb-0 fw-medium">วันนี้ยังไม่ได้บันทึกกิจกรรมบ่าย</p>
                <span class="text-muted text-xs">2 ชั่วโมงที่แล้ว</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- User Profile -->
      <div class="dropdown">
        <a class="nav-link dropdown-toggle d-flex align-items-center gap-2" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <div class="bg-hospital-light text-teal fw-bold rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; font-size: 0.85rem;">
            <%= typeof user !== 'undefined' ? user.fullName.substring(0,2) : 'ผู้ใช้' %>
          </div>
          <span class="fw-medium text-dark d-none d-sm-inline" style="font-size: 0.9rem;">
            <%= typeof user !== 'undefined' ? user.fullName : 'ผู้ใช้ระบบ' %>
          </span>
        </a>
        <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-card mt-2 p-2" aria-labelledby="navbarDropdown">
          <li>
            <div class="px-3 py-2">
              <p class="mb-0 fw-semibold text-dark text-sm"><%= typeof user !== 'undefined' ? user.fullName : 'ผู้ใช้ระบบ' %></p>
              <span class="text-muted text-xs">สิทธิ์: <%= typeof user !== 'undefined' ? user.role : 'ผู้เยี่ยมชม' %></span>
            </div>
          </li>
          <li><hr class="dropdown-divider my-1"></li>
          <li><a class="dropdown-item rounded-2 py-2" href="/settings"><i class="bi bi-gear me-2 text-secondary"></i>ตั้งค่าบัญชี</a></li>
          <li><hr class="dropdown-divider my-1"></li>
          <li><a class="dropdown-item text-danger rounded-2 py-2" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>ออกจากระบบ</a></li>
        </ul>
      </div>
    </div>
  </div>
</nav>
